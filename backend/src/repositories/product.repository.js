export const productRepository = {
  addProduct: async (name, price) => {
    try {
      // TODO: write to master
    } catch (e) {
      throw e;
    }
  },

  getProducts: async () => {
    try {
      // TODO: read from slave
    } catch (e) {
      throw e;
    }
  }
}